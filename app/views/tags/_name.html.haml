%span.actions== #{link_to("Rename", "#", onclick: "Tags.rename(#{tag_path(tag_ref).to_json}, #{tag_ref.name.to_json}, #{form_authenticity_token.to_json})")} | #{link_to("Delete", "#", onclick: "Tags.deleteTag()")}
== Transactions tagged "#{h(tag_ref.name)}"
