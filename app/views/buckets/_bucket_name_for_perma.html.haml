%span.actions== #{link_to("Rename", "#", onclick: "Buckets.rename(#{bucket_path(bucket).to_json}, #{bucket.name.to_json}, #{form_authenticity_token.to_json})")} | #{link_to("Delete/Merge", "#", onclick: "Buckets.deleteBucket()")}
== #{link_to(h(bucket.account.name), bucket.account)}:
&= bucket.name
