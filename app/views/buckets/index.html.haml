#data.content
  %h2
    = balance_cell(account, :classes => %w(total balance), :tag => "span")
    &= account.name
    = back_to_dashboard_link

  %table.account_summary
    = render(account.buckets.sort_by(&:name))
