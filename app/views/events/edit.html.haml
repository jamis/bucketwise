:javascript
  Events.return_to = "#{(params[:return_to] || subscription_path(subscription))}";

#data.content
  %h2 Edit transaction

  .transaction.edit.form{:class => @event.role.to_s}= render :partial => "events/form"
